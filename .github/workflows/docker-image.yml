name: Docker
on:
  push:
  
jobs:
  check-updates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
        id: "rust-toolchain"
      - uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/git
            ~/.cargo/registry
            targe
          key: "${{runner.os}} Rust ${{steps.rust-toolchain.outputs.cachekey}} Lock ${{hashFiles('Cargo.lock')}}"
      - run: cargo run
        env:
          RUST_BACKTRACE: 1
      - run: cat output.txt >> $GITHUB_OUTPUT
        id: output
      - run: cat output.txt
      - run: echo "matrix=test" >> $GITHUB_OUTPUT
        id: test
      
  test:
    runs-on: ubuntu-latest
    needs: [check-updates]
    steps:
      - name: Echo matrix
        run: echo "${{ needs.check-updates.output.matrix }}"
      - name: test
        run: echo "${{ needs.check-updates.test.matrix }}"
          
  docker:
    runs-on: ubuntu-latest
    needs: [check-updates]
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.check-updates.output.matrix) }}
    steps:
      - name: Echo matrix
        run: |
          echo "${{ matrix.tag }}"
          echo "${{ matrix.path }}
